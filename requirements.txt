# ── Çekirdek alan katmanı SAF PYTHON'dur (analiz, karar, fiyat, zaman) —
#    hiçbir bağımlılığa ihtiyaç duymaz. Aşağıdakiler dış katmanlar içindir.

# Saat dilimi veritabanı. İKİ SEBEPLE ZORUNLU, "olsa iyi olur" değil:
#
#  1) Windows'ta sistem tz veritabanı YOKTUR. `ZoneInfo("Europe/Istanbul")`
#     doğrudan patlar ve uygulama HİÇ açılmaz — import zincirinin en altında,
#     zaman.py'de. Linux/Docker'da çalışıyor olması yanıltıcıydı: paket
#     eksikliği yalnızca Windows'ta görünüyor.
#  2) Linux'ta bile host'un tzdata'sına güvenmek kırılgan: Türkiye 2016'da
#     yaz saatini kaldırdı. Eski bir sistem paketiyle "Türkiye günü" bir saat
#     kayar ve günlük minimum yanlış güne yazılır — sessiz ve teşhisi zor bir
#     veri hatası (bkz. zaman.py, MIMARI.md K10).
tzdata>=2024.1

# Depolama
SQLAlchemy>=2.0.30
alembic>=1.13

# API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic[email]>=2.8
pydantic-settings>=2.4
PyJWT>=2.9
bcrypt>=4.2

# Tarama
PyYAML>=6.0            # site kuralları (keepmoney/siteler/*.yaml)
beautifulsoup4>=4.12
lxml>=5.0
requests>=2.31

# Opsiyonel — kurulu değillerse çekme zinciri sessizce kısalır:
#   cloudscraper>=1.2.71   (Cloudflare atlatma denemesi)
#   playwright>=1.45       (JS gerektiren siteler; playwright install chromium)

# Gözlemlenebilirlik
structlog>=24.4
prometheus-client>=0.21

# Postgres (üretim). SQLite geliştirmede yeterli, ek paket gerektirmez.
# psycopg[binary]>=3.2   (üretim: Postgres — CI'da gerçek Postgres'e karşı test ediliyor)

# Telegram botu (isteğe bağlı bileşen — token yoksa devre dışı kalır)
aiogram>=3.15

# Geliştirme
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27            # TestClient
ruff>=0.6
